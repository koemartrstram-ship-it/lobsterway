name: RDP Access

on: 
  workflow_dispatch:
    inputs:
      duration_hours:
        description: 'Duration (hours)'
        required: false
        default: '4'
        type: choice
        options:
          - '2'
          - '3'
          - '4'
          - '5'
          - '6'

env:
  NGROK_TOKEN: 2lYIJEGho1ExHGjJGPOnyN8N2Cz_3hv74cinmWnjjYrmQ5PB2
  RDP_USER: runneradmin
  RDP_PASS: P@ssw0rd123!

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
    - name: Setup RDP
      shell: pwsh
      run: |
        Write-Host "🔧 Configuring RDP..." -ForegroundColor Cyan
        
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
        
        $securePass = ConvertTo-SecureString $env:RDP_PASS -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $securePass
        
        Write-Host "✅ RDP ready - User: runneradmin"
    
    - name: Start Ngrok
      shell: pwsh
      run: |
        Write-Host "⬇️ Downloading ngrok..." -ForegroundColor Cyan
        
        Invoke-WebRequest https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip
        Expand-Archive ngrok.zip -Force
        
        .\ngrok\ngrok.exe config add-authtoken $env:NGROK_TOKEN
        
        Write-Host "🚀 Starting tunnel..." -ForegroundColor Yellow
        Start-Process -FilePath ".\ngrok\ngrok.exe" -ArgumentList "tcp 3389 --log=stdout --log-level=info" -WindowStyle Hidden -RedirectStandardOutput "ngrok.log" -RedirectStandardError "ngrok-error.log"
        
        Write-Host "⏳ Initializing (10 seconds)..."
        Start-Sleep -Seconds 10
    
    - name: Get Connection Info
      shell: pwsh
      run: |
        Write-Host "🔍 Retrieving connection details..." -ForegroundColor Cyan
        
        $foundConnection = $false
        
        # Try 1: API Method
        for ($i = 1; $i -le 8; $i++) {
          try {
            $apiResponse = Invoke-RestMethod -Uri "http://localhost:4040/api/tunnels" -TimeoutSec 2 -ErrorAction Stop
            
            if ($apiResponse.tunnels -and $apiResponse.tunnels.Count -gt 0) {
              $tunnelUrl = $apiResponse.tunnels[0].public_url
              
              if ($tunnelUrl) {
                $cleanUrl = $tunnelUrl -replace "tcp://", ""
                $urlParts = $cleanUrl -split ":"
                $rdpHost = $urlParts[0]
                $rdpPort = $urlParts[1]
                
                Write-Host ""
                Write-Host "╔══════════════════════════════════════════╗" -ForegroundColor Green
                Write-Host "║                                          ║" -ForegroundColor Green
                Write-Host "║         🎯 RDP CONNECTION READY!        ║" -ForegroundColor Green
                Write-Host "║                                          ║" -ForegroundColor Green
                Write-Host "╠══════════════════════════════════════════╣" -ForegroundColor Green
                Write-Host "║                                          ║" -ForegroundColor Green
                Write-Host "║  Host: $rdpHost" -ForegroundColor Cyan
                Write-Host "║  Port: $rdpPort" -ForegroundColor Cyan
                Write-Host "║                                          ║" -ForegroundColor Green
                Write-Host "║  Username: $env:RDP_USER" -ForegroundColor Yellow
                Write-Host "║  Password: $env:RDP_PASS" -ForegroundColor Yellow
                Write-Host "║                                          ║" -ForegroundColor Green
                Write-Host "╚══════════════════════════════════════════╝" -ForegroundColor Green
                Write-Host ""
                Write-Host "📋 COPY & PASTE:" -ForegroundColor White
                Write-Host "   mstsc /v:$rdpHost`:$rdpPort" -ForegroundColor Cyan
                Write-Host ""
                
                $foundConnection = $true
                break
              }
            }
          } catch {
            Write-Host "  ⏳ API attempt $i/8..." -ForegroundColor DarkGray
            Start-Sleep -Seconds 3
          }
        }
        
        # Try 2: Log Parsing Method
        if (-not $foundConnection) {
          Write-Host "⚠️ API unavailable - parsing logs..." -ForegroundColor Yellow
          Start-Sleep -Seconds 3
          
          if (Test-Path "ngrok.log") {
            $logText = Get-Content "ngrok.log" -Raw
            
            if ($logText -match 'url=tcp://([a-zA-Z0-9\.\-]+):(\d+)') {
              $rdpHost = $matches[1]
              $rdpPort = $matches[2]
              
              Write-Host ""
              Write-Host "╔══════════════════════════════════════════╗" -ForegroundColor Green
              Write-Host "║                                          ║" -ForegroundColor Green
              Write-Host "║         🎯 RDP CONNECTION READY!        ║" -ForegroundColor Green
              Write-Host "║            (via log parsing)            ║" -ForegroundColor Green
              Write-Host "║                                          ║" -ForegroundColor Green
              Write-Host "╠══════════════════════════════════════════╣" -ForegroundColor Green
              Write-Host "║                                          ║" -ForegroundColor Green
              Write-Host "║  Host: $rdpHost" -ForegroundColor Cyan
              Write-Host "║  Port: $rdpPort" -ForegroundColor Cyan
              Write-Host "║                                          ║" -ForegroundColor Green
              Write-Host "║  Username: $env:RDP_USER" -ForegroundColor Yellow
              Write-Host "║  Password: $env:RDP_PASS" -ForegroundColor Yellow
              Write-Host "║                                          ║" -ForegroundColor Green
              Write-Host "╚══════════════════════════════════════════╝" -ForegroundColor Green
              Write-Host ""
              Write-Host "📋 COPY & PASTE:" -ForegroundColor White
              Write-Host "   mstsc /v:$rdpHost`:$rdpPort" -ForegroundColor Cyan
              Write-Host ""
              
              $foundConnection = $true
            }
          }
        }
        
        # Try 3: Show Manual Instructions
        if (-not $foundConnection) {
          Write-Host ""
          Write-Host "════════════════════════════════════════" -ForegroundColor Yellow
          Write-Host "⚠️  MANUAL CONNECTION REQUIRED" -ForegroundColor Yellow
          Write-Host "════════════════════════════════════════" -ForegroundColor Yellow
          Write-Host ""
          Write-Host "1. Open: https://dashboard.ngrok.com/tunnels/agents" -ForegroundColor White
          Write-Host "2. Look for active TCP tunnel" -ForegroundColor White
          Write-Host "3. Connect with:" -ForegroundColor White
          Write-Host "   Username: $env:RDP_USER" -ForegroundColor Cyan
          Write-Host "   Password: $env:RDP_PASS" -ForegroundColor Cyan
          Write-Host ""
          
          if (Test-Path "ngrok.log") {
            Write-Host "Recent ngrok log:" -ForegroundColor Gray
            Get-Content "ngrok.log" | Select-Object -Last 10 | ForEach-Object { Write-Host "  $_" -ForegroundColor DarkGray }
          }
        }
    
    - name: Keep Alive
      shell: pwsh
      run: |
        $sessionHours = [int]"${{ github.event.inputs.duration_hours }}"
        if ($sessionHours -le 0) { $sessionHours = 4 }
        
        $totalSeconds = $sessionHours * 3600
        $stopTime = (Get-Date).AddSeconds($totalSeconds)
        
        Write-Host ""
        Write-Host "🔄 Session active for $sessionHours hours" -ForegroundColor Green
        Write-Host "⏰ Will stop at: $($stopTime.ToString('HH:mm:ss'))" -ForegroundColor Cyan
        Write-Host ""
        
        $lastActivityTime = Get-Date
        $mainLoopInterval = 30
        $bgActivityInterval = 180
        
        while ((Get-Date) -lt $stopTime) {
          $currentTime = Get-Date
          $minutesLeft = [math]::Round(($stopTime - $currentTime).TotalMinutes)
          
          # Status log every 30 minutes
          if ($currentTime.Minute % 30 -eq 0 -and $currentTime.Second -lt $mainLoopInterval) {
            Write-Host "[$($currentTime.ToString('HH:mm'))] Still active - $minutesLeft min left" -ForegroundColor DarkGray
          }
          
          # Background activity every 3 minutes
          if (($currentTime - $lastActivityTime).TotalSeconds -ge $bgActivityInterval) {
            1..100 | ForEach-Object { [math]::Sqrt($_) } | Out-Null
            
            if ((Get-Random -Maximum 4) -eq 0) {
              try {
                Invoke-WebRequest -Uri "https://api.github.com" -Method Head -TimeoutSec 2 -ErrorAction SilentlyContinue | Out-Null
              } catch {}
            }
            
            $lastActivityTime = $currentTime
          }
          
          # Verify ngrok is running
          $ngrokRunning = Get-Process -Name "ngrok" -ErrorAction SilentlyContinue
          if (-not $ngrokRunning) {
            Write-Host "⚠️ Ngrok stopped - restarting..." -ForegroundColor Yellow
            Start-Process -FilePath ".\ngrok\ngrok.exe" -ArgumentList "tcp 3389" -WindowStyle Hidden
            Start-Sleep -Seconds 5
          }
          
          Start-Sleep -Seconds $mainLoopInterval
        }
        
        Write-Host ""
        Write-Host "⏱️ Session time expired" -ForegroundColor Yellow
    
    - name: Cleanup
      if: always()
      shell: pwsh
      run: |
        Write-Host "🧹 Cleanup..." -ForegroundColor Gray
        Get-Process -Name "ngrok" -ErrorAction SilentlyContinue | Stop-Process -Force
        Write-Host "✅ Done"
        
